import React, { useEffect, useState } from "react";
import { VIDEO_DETAILS_API } from "../utils/constants";

const VideoInfo = ({ video }) => {
  console.log(video);
  return <div>--title --channel title</div>;
};

export default VideoInfo;
